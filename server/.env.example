# MongoDB Atlas connection string (required in Render Environment settings)
MONGO_URI=mongodb+srv://USERNAME:PASSWORD@cluster.mongodb.net/snapticket?retryWrites=true&w=majority
# Comma-separated web origins allowed by CORS; mobile apps do not need an origin.
CLIENT_URL=http://localhost:5173
